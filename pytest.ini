[pytest]
pythonpath = .

markers =
    unit: fast in-process tests with no HTTP or browser
    integration: tests that exercise the HTTP API layer
    e2e: end-to-end tests that require a running server
